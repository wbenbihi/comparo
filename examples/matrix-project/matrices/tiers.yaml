apiVersion: comparo/v1
kind: Matrix
metadata:
  name: Plan tiers
  id: matrix.tiers
  description: Two subscription tiers, merged into the query string.
spec:
  target: request.query
  mode: merge
  values:
    - tier: free
    - tier: pro
