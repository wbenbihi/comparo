apiVersion: comparo/v1
kind: Request
metadata:
  name: Ping
  id: request.ping
  description: A single-cell status check with no matrix — the baseline case.
  tags:
    - health
spec:
  request:
    method: GET
    endpoint: /status/200
  response:
    status: 200
