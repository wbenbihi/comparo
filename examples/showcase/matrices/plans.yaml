apiVersion: comparo/v1
kind: Matrix
metadata:
  name: Plans
  id: matrix.plans
  description: Quote every pricing plan — three cells per quote request.
spec:
  target: request.query
  mode: merge
  values:
    - plan: basic
    - plan: pro
    - plan: scale
