apiVersion: comparo/v1
kind: Request
metadata:
  name: Quote history
  id: request.history
  description: /json is static — deselect this one at Prepare to see the ⊘ roster.
  tags:
    - pricing
spec:
  request:
    method: GET
    endpoint: /json
  response:
    status: 200
