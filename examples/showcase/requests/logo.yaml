apiVersion: comparo/v1
kind: Request
metadata:
  name: PNG image
  id: request.logo
  description: /image/png is static binary — magic · sha256 · hex on the Run tab.
  tags:
    - pages
spec:
  request:
    method: GET
    endpoint: /image/png
  response:
    status: 200
