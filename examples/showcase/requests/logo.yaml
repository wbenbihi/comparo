apiVersion: comparo/v1
kind: Request
metadata:
  name: Logo
  id: request.logo
  description: Identical binary — honest bytes (magic · sha256 · hex) in the Run detail.
  tags:
    - pages
spec:
  request:
    method: GET
    endpoint: /logo
  response:
    status: 200
